{
  "name": "stallning",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "lint": "turbo run lint",
    "lint:oxlint": "turbo run lint:oxlint",
    "lint:eslint": "turbo run lint:eslint",
    "deps:check:minor": "pnpx taze minor -r",
    "deps:check:major": "pnpx taze major -r",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "check-types": "turbo run check-types",
    "prepare": "husky || true",
    "pre-commit": "lint-staged",
    "changeset": "pnpm exec changeset",
    "changeset:conventional-commits": "node --experimental-strip-types ./.changeset/changeset-conventional-commits.ts",
    "changeset:release": "pnpm exec changeset tag",
    "build": "turbo run build"
  },
  "dependencies": {
    "@changesets/cli": "^2.30.0"
  },
  "devDependencies": {
    "@changesets/read": "^0.6.7",
    "@changesets/types": "^6.1.0",
    "@changesets/write": "^0.4.0",
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@manypkg/get-packages": "^3.1.0",
    "@stallning/eslint": "workspace:^",
    "@stallning/oxfmt": "workspace:^",
    "@stallning/oxlint": "workspace:^",
    "@stallning/typescript": "workspace:^",
    "@types/node": "^25.5.2",
    "commander": "^14.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxlint": "catalog:",
    "turbo": "^2.9.5"
  },
  "lint-staged": {
    "**/*.{json,js,ts,md,yaml,yml}": [
      "oxlint --fix",
      "eslint --fix",
      "oxfmt --write"
    ]
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "header-max-length": [
        2,
        "always",
        100
      ],
      "scope-enum": [
        2,
        "always",
        [
          "global",
          "config"
        ]
      ],
      "type-enum": [
        2,
        "always",
        [
          "build",
          "chore",
          "ci",
          "docs",
          "feat",
          "fix",
          "perf",
          "refactor",
          "revert",
          "style",
          "test"
        ]
      ]
    }
  },
  "engines": {
    "node": ">=25.8.0"
  },
  "packageManager": "pnpm@10.33.0"
}
