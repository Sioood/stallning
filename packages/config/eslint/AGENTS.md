# @stallning/eslint

ESLint flat-config factory (`eslint()`). See [../AGENTS.md](../AGENTS.md).

- Entry: `index.ts`
- Prefer new rules in `@stallning/oxlint` first; bridge with `eslint-plugin-oxlint`
- Formatting is oxfmt only (`eslint-config-prettier` turns off overlapping stylistic rules)
